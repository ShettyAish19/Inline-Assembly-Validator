# Static Inline Assembly Validator for x86-64

An educational static analysis tool that parses and validates inline assembly strings inside LLVM Intermediate Representation (IR) before compilation.

## Requirements
- Ubuntu/Debian (or Windows WSL)
- Clang/LLVM
- g++ (C++17 support)

## How to Run

1. **Build the project:**
   ```bash
   ./build.sh